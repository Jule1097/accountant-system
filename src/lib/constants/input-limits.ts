export const inputLimits = {
  maxNameLength: 255,
  maxSearchLength: 255,
  maxFreeTextLength: 5000,
  maxBulkItemIds: 1000,
  maxParserFiles: 20,
  maxParserRequestBytes: 24 * 1024 * 1024,
} as const
